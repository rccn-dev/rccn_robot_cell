{
    "name": "ROS 2 Jazzy",
    "image": "osrf/ros:jazzy-desktop",
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "runArgs": [
        "--network=host",
        "--ipc=host",
        "--pid=host",
        "--privileged"
    ],
    "containerEnv": {
        "DISPLAY": "host.docker.internal:0",
        "RMW_IMPLEMENTATION": "rmw_cyclonedds_cpp",
        "CYCLONEDDS_URI": "/workspaces/${localWorkspaceFolderBasename}/cyclonedds.xml",
        "ROS_DOMAIN_ID": "100"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-iot.vscode-ros",
                "ms-python.python",
                "ms-vscode.cpptools",
                "twxs.cmake"
            ]
        }
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y python3-colcon-common-extensions python3-vcstool python3-rosdep ros-jazzy-rmw-cyclonedds-cpp && grep -qxF 'source /opt/ros/jazzy/setup.bash' ~/.bashrc || echo 'source /opt/ros/jazzy/setup.bash' >> ~/.bashrc && git config --global --add safe.directory ${containerWorkspaceFolder} && cd ${containerWorkspaceFolder} && bash scripts/bootstrap_workspace.sh"
}
